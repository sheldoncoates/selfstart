// import { Pipe, PipeTransform } from '@angular/core';

// @Pipe({
//   name: 'sort'
// })
// export class SortPipe implements PipeTransform {

//   // transform(array: any[], fn: Function = (a,b) => a > b ? 1 : -1): any {
//   //   return array.sort(fn);
//   // }
// }
